---
import { ViewTransitions } from 'astro:transitions';
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Hector Garcia - Front-end Developer Portfolio" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body class="bg-slate-950 text-slate-200 antialiased min-h-screen w-full overflow-x-hidden">
		<slot />
	</body>
</html>

<style is:global>
	/* Global styles if needed, Tailwind handles most */
	:root {
		--accent: 6, 182, 212; /* Cyan-500 */
	}
	html {
		font-family: system-ui, sans-serif;
	}
</style>
